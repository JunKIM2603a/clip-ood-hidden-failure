# Dataset-installer dependencies.
# Keep these aligned with requirements/experiment.txt so installing datasets
# cannot silently change a verified experiment environment.
huggingface-hub==0.35.3
datasets==4.1.1
gdown==5.2.0
